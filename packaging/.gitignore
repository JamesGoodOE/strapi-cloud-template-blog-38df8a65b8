# Dependencies and build output
node_modules/
build/

# Copied/generated artifacts (regenerated by build.sh)
dist/
certs/
*.zip
/AskOE-localhost.crt
/manifest.xml

# Generated bundle binaries — only the source files in bundle/ are tracked
bundle/*.exe
bundle/*.crt
bundle/manifest.xml
